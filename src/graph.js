// graph.js is a placeholder for future relationship graph logic.
// Currently, we are focusing on the Grid and Table views.
window.graph = {
    renderRelationshipMap: (container, topicId) => {
        console.log(`Rendering relationship map for ${topicId}`);
        container.innerHTML = `<div class="text-center p-8 text-slate-500">關係圖功能開發中...</div>`;
    }
};
